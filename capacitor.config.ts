import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vintagephotobooth.app',
  appName: 'Vintage Photobooth',
  webDir: 'dist'
};

export default config;
